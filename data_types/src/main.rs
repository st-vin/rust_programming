fn main() {
    // Data type subsets include scalar and compound

    let _guess: u8 = "40".parse().expect("Not a number!");

    // scalar represent a single value, they include: integers, floating point
    // floating point, booleans and characters.

    // floating point numbers
    let i = 2.2;
    let j = 3.8;

    let _sum = i + j;

    // Compound types can group multiple values into one type.
    //Rust has two primitive compound types: tuples and arrays
}
